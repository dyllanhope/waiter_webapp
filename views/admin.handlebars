<div class='row'>
    <div class='col-12 center'>
        <h1>Waiter Data</h1>
    </div>
</div>
<div class='row'>
    <div class='col-12 center'>
        <table>
            <tr>
                {{#each days}}
                <th>
                    <h4 class='{{style}}' style="float: none; text-align: center"><u>{{day}}</u> <br> waiters:
                        {{waiters}}</h4>
                </th>
                {{/each}}
            </tr>
            <tr>
                {{#each workers}}
                <td>
                    {{#each waiters}}
                    <form action="/deleteWaiter/{{this}}-{{../day}}" method='POST'>
                        <span style="float: right">{{this}}
                            <input class='deleteBtn' type="submit" value=" - "
                                onclick="return confirm('Are you sure you want to remove them from this day?')">
                        </span> <br>
                    </form>
                    {{/each}}
                </td>
                {{/each}}
            </tr>
        </table>
        </a>
    </div>
</div>
<div class='row'>
    <div class='col-4 center'>
    </div>
    <div class='col-4 center'>
        <form action="/clear" method="POST">
            <input type="submit" class="btnStyle" style='width:130px' value="Clear shifts">
        </form>
        <form action="/">
            <input type="submit" class="btnStyle" style='width:130px' value="Login screen">
        </form>
    </div>
    <div class='col-4'>
        <h3>Waiters that haven't chosen shifts:</h3>
        {{#each notWorking}}
        <a href="/update/{{this}}">{{this}}</a> <br>
        {{/each}}
    </div>
</div>